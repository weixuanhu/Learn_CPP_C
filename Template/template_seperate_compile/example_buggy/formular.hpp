#ifndef FORMULAR_H
#define FORMULAR_H

// #define GLOBAL_MAX 1e+10
// #define GLOBAL_MIN -1e+10

template <typename T> T getMin(T, T);
template <typename T> T getMax(T, T);
template <typename T> T getMean(T, T);
template <typename T> T getRange(T, T);

#endif